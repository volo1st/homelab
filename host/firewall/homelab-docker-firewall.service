[Unit]
Description=Homelab Docker ingress firewall
Requires=docker.service
After=docker.service ufw.service
PartOf=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/homelab-docker-firewall --apply
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
