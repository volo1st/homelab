# Storage

Host-level storage configuration belongs here.

Current known assumptions:

- The core host has four M.2 SSD slots.
- The NAS share points at `/mnt/nas_pool`.
- The existing Samba config references a mergerfs pool.

Track mount units, filesystem setup, mergerfs configuration, and storage health checks here when they are added.
