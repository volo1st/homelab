# Services

Docker Compose based services belong here.

Each service should live in its own directory when it needs more than a single compose file, such as config mounts, environment templates, backup notes, or health checks.
